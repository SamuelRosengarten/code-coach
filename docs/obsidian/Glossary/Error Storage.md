Where and how error events are persisted, as JSON. Tasks: *Pick storage location*, *Log error event when JSON*, *Add config entry for repeat threshold and time window*. See [[Error|Diagnostics]].

Part of [[Code Coach]].
